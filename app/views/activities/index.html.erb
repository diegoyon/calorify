<h1>Activities</h1>

<%= current_user.activity_counter %>

<div>
  <% @activities.each do |activity| %>
    <%= render activity %>
  <% end %>
</div>

<%= link_to "New activity", new_activity_path %>
