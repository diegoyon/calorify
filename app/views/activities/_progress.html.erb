<% consumed = current_user.activities.where(date: Date.current, burned: false).sum(:calories) %>
<% total = current_user.calorie_goal %>
<% color = set_color(consumed, total) %>

<div class="block px-5">
  <progress class="progress <%= color %>" value="<%= consumed %>" max="<%= total%>"></progress>
</div>